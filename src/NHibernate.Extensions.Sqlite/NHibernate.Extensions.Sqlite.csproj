<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <PackageVersion>2.0.0</PackageVersion>
    <Title>NHibernate.Extensions.Sqlite</Title>
    <PackageReleaseNotes>Initial release, since it is a .NET Standard 2.0 library, so the version starts with 2.0.0 .</PackageReleaseNotes>
    <Authors>beginor</Authors>
    <Description>NHibernate Sqlite Driver with Microsoft.Data.Sqlite.Core.
      In order to use this driver, you must have the nuget package
      [Microsoft.Data.Sqlite.Core](https://www.nuget.org/packages/Microsoft.Data.Sqlite.Core/)
      and [SQLitePCLRaw.bundle_e_sqlite3](https://www.nuget.org/packages/SQLitePCLRaw.bundle_e_sqlite3/)
      installed for NHibernate to load.
      It's a .NET Standard 2.0 Driver, Can run on any platform that dotnet core support.
    </Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="NHibernate" Version="5.2.7" />
  </ItemGroup>

</Project>
